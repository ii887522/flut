[package]
name = "flut_codegen"
version = "0.1.0"
edition = "2021"
rust-version = "1.80"
publish = false

[lib]
proc-macro = true

[dependencies]
syn = { version = "2.0", features = ["full"] }
quote = { version = "1.0", default-features = false }
